CompileFlags:
  # Keep standalone headers out of clangd's C++98 fallback. Source files obtain
  # their full command line from the root compile_commands.json generated by
  # the currently selected CMake clangd preset.
  Add: [ -std=c++23 ]

Diagnostics:
  ClangTidy:
    Remove:
    - "*"
