[Settings]
bFrameGenerationMode = true
bFrameLimitMode = true
; Real rendered FPS target. 0 uses automatic policy: unlimited only when
; High FPS Physics Fix is loaded and UntieSpeedFromFPS=true; otherwise 60 FPS.
; Positive values are clamped to 30-1000 FPS and are never halved for FrameGen.
fRealFrameRateLimit = 0.0
iFrameGenerationBackend = 2
bDebugLogging = false
iStreamlineLogLevel = 1
iDebugFrameLogCount = 120
